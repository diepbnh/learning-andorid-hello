# Notes

- Người học là Designer, giỏi HTML/CSS, chưa từng viết code logic ở bất kỳ ngôn ngữ nào (đã xác nhận trực tiếp). Không giả định họ hiểu biến/hàm/điều kiện — phải dạy tường minh khi lần đầu xuất hiện.
- Viết lesson bằng **Tiếng Việt**. Giữ nguyên tiếng Anh cho thuật ngữ kỹ thuật riêng của Compose/Android (Composable, Modifier, State, Recomposition, Preview...) — không dịch, vì tài liệu chính thức và cộng đồng đều dùng tiếng Anh.
- Luôn tận dụng phép so sánh HTML/CSS ⇄ Compose khi giới thiệu khái niệm mới (ví dụ: Modifier ~ inline style/CSS chain, Row/Column ~ flexbox, Composable function ~ HTML component/React component). Đây là cầu nối chính giúp giảm tải nhận thức.
- Project "hello" đã có sẵn code (LoginPage, ResetPasswordPage, AppButton, AppTextField, ThemeManager) — có vẻ được viết với sự hỗ trợ AI trước đây (comment tiếng Việt, một số code chưa idiomatic). Không giả định người học tự viết/hiểu được các file này — coi như "code có sẵn" để đọc và học từ đó, chưa phải bằng chứng đã hiểu Compose.
- Repo không phải git repository (theo môi trường hiện tại) — không có lịch sử commit để tham khảo tiến độ.
